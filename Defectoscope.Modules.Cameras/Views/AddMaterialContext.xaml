<UserControl x:Class="Defectoscope.Modules.Cameras.Views.AddMaterialContext"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"             
             prism:ViewModelLocator.AutoWireViewModel="True">
    <!--#region Material selection -->
    <DockPanel LastChildFill="False">
        <Grid DockPanel.Dock="Bottom">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="60" />
                <RowDefinition Height="60"/>
                <RowDefinition Height="40"/>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="5,10,5,10"/>
            <Button Width="80" Margin="2,10,2,2" Grid.Column="0" Grid.Row="2" Content="Добавить"  Command="{Binding }"/>
            <Button Width="80" Margin="2,10,2,2" Grid.Column="1" Grid.Row="2" Content="Удалить"  Command="{Binding }"/>
        </Grid>
    </DockPanel>
    <!--#endregion-->
</UserControl>
